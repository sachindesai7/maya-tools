# Tool Spec: [Tool Name]

## What It Does
One paragraph. Plain English. No jargon.

## Inputs
- **Selection required:** mesh / joint / curve / nothing
- **UI controls:** list each slider, dropdown, checkbox with its range/options and default value

## Outputs
- What nodes/groups get created
- Naming pattern (e.g. `{mesh}_LOD0`, `{mesh}_LOD1`)
- Where in the outliner (parented to what)

## Step-by-Step Logic
1. Validate selection
2. ...
3. ...

## Edge Cases
- What happens if nothing is selected
- What happens if the wrong type is selected
- What happens if the tool is run twice on the same object

## Maya Icon
Suggested built-in icon name (see STANDARDS.md section 2)

## Shelf Label
Two-line shelf label text (keep very short)
